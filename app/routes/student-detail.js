import Route from '@ember/routing/route';

export default class StudentDetailRoute extends Route {
  model(params) {
    // const { student_id } = params;
    // return student_id;
    // return this.store.findRecord('student', params.student_id);
  }
}
