import Route from '@ember/routing/route';

export default class StudentEditRoute extends Route {
  //   model(params) {
  //     //return params.student_id; // Assuming student_id is the parameter used to identify the student
  //     return params;
  //   }
}
